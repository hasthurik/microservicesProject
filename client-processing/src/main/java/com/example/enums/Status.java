package com.example.enums;

//product_status_enum
public enum Status {
    ACTIVE("active"),
    CLOSED("closed"),
    BLOCKED("blocked"),
    ARRESTED("arrested");

    Status(String value) {
    }
}
